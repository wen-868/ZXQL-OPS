# 踩坑日志

> 全员维护。每次任务前后必读，避免重复踩坑。
> 格式见 `项目规则.md` 第五章。任何花超过 30 分钟定位的问题都须记录。

（暂无记录。首个踩坑由对应成员在任务完成后补充。）

### 2026-08-09 · 阿澜 · 8 页面样式重构 · 经验记录
- **无阻塞坑**：本次仅修改 `<style scoped>` 中的 CSS 变量替换，不动 template 和 script，8 页均一次通过。
- **构建中 TS 报错**：`npm run build` 报 `TS2305 Module has no exported member`、`TS6133 declared but never read` 等均为项目中预先存在的类型问题（API 导出未对齐、未使用 import），与本次样式替换无关。
- **经验**：设计规范 §10 和附录 A 的 CSS 变量体系覆盖完整（间距/圆角/字号/色彩），替换时只需查表映射即可，效率高且不易出错。
