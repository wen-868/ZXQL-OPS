# 数据库变更清单

> 舟行维护。记录每个迁移脚本的执行状态、日期、验证结果。
> 迁移脚本放 `docs/migrations/NNN_*.sql`，须带 `IF NOT EXISTS` 保护与验证 SQL。
> 详见 `项目规则.md` 第十二章 防线3。

| 序号 | 脚本 | 状态 | 执行日期 | 验证结果 |
|------|------|------|----------|----------|
| 001 | 001_account_matrix.sql（ops_accounts / ops_account_health_events） | 待执行 | | |
| 002 | 002_intel.sql（ops_competitors / ops_collected_comments / ops_collect_tasks / ops_hot_snapshots） | 待执行 | | |
| 003 | 003_analyze.sql（ops_analysis_tasks / ops_human_insights） | 待执行 | | |
| 004 | 004_topic.sql（ops_topics） | 待执行 | | |
| 005 | 005_script.sql（ops_scripts） | 待执行 | | |
| 006 | 006_publish.sql（ops_publish_tasks） | 待执行 | | |
| 007 | 007_workflow.sql（ops_workflow_defs/runs/logs） | 待执行 | | |
| 008 | 008_recycle.sql（ops_feedback / ops_recycle_tasks / ops_driver_efficiency） | 待执行 | | |
| 009 | 009_n_role.sql（ops_roles / ops_role_user / ops_audit_logs） | 待执行 | | |
| 010 | 010_t_selection.sql（ops_selection_products / ops_selection_lists） | 待执行 | | |
| 011 | 011_r_product.sql（ops_products / ops_product_contents / ops_product_detail_pages） | 待执行 | | |
| 012 | 012_k_live.sql（ops_live_rooms / ops_digital_humans / ops_live_danmu / ops_live_ai_replies / ops_live_stats） | 待执行 | | |
| 013 | 013_s_ad.sql（ops_ad_accounts / ops_ad_campaigns / ops_ad_metrics） | 待执行 | | |
| 014 | 014_u_private.sql（ops_fans_profiles / ops_private_groups） | 待执行 | | |
| 015 | 015_w_revenue.sql（ops_revenue_records / ops_reconciliations / ops_settlements） | 待执行 | | |
| 016 | 016_y_order.sql（ops_orders / ops_logistics_tracks / ops_waybills） | 待执行 | | |
| 017 | 017_aa_customer_service.sql（ops_customer_sessions / ops_customer_messages / ops_support_tickets / ops_knowledge_base / ops_cs_settings） | 待执行 | | |
| 018 | 018_dashboard.sql（ops_dashboards） | 待执行 | | |
| 019 | 019_materials.sql（ops_materials） | 待执行 | | |
| 020 | 020_videos.sql（ops_videos） | 待执行 | | |

## 001_account_matrix.sql（B-core 账号矩阵）
- 实体：`ops_accounts`（账号矩阵，含加密 token 字段 `token_enc`/`refresh_token_enc`、`status` 健康状态、身份/赛道/阶段分组、粉丝/关注/获赞快照）
- 实体：`ops_account_health_events`（账号健康事件：掉签/限流/降权/恢复/封禁/重新授权留痕）
- 唯一约束：`(tenant_id, platform, platform_account_id)`
- 索引：`ops_account_health_events(tenant_id, account_id, created_at)`
- 由 TypeORM 同步生成（AUTO=True），也可落 `docs/migrations/001_account_matrix.sql` 供 CI/生产显式执行。

## 002_intel.sql（C 情报采集）
- 实体：`ops_competitors`（竞品：platform/name/url/category/monitorEnabled/lastCollectedAt/healthScore）
- 实体：`ops_collected_comments`（采集评论：platform/sourceType/sourceRef/content/authorId/likes/isClean/cleanResult/contentHash/collectedAt/taskId；contentHash 去重索引）
- 实体：`ops_collect_tasks`（采集任务：type/target/platform/sourceLevel/status/progress/collectedCount/scope/fieldsCollected/errorMsg/finishedAt）
- 实体：`ops_hot_snapshots`（热点快照：platform/hotType/title/heat/url/capturedAt）
- 索引：`ops_collected_comments(tenant_id, content_hash)`、`ops_collected_comments(tenant_id, task_id)`、`ops_competitors(tenant_id)`、`ops_collect_tasks(tenant_id, status)`
- 合规：`collected_comments` 仅落公开字段白名单；content 命中隐私已剥离为 `[已脱敏]`，cleanResult 记录 piiRemoved/ad
- 由 TypeORM 同步生成（AUTO=True），也可落 `docs/migrations/002_intel.sql` 供 CI/生产显式执行。

## 003_analyze.sql（D 人性分析）
- 实体：`ops_analysis_tasks`（分析任务：source/platform/inputRefs/status/progress/totalComments/driverCounts/emotionScores/topDrivers/topEmotions/insights/modelUsed/promptVersion/errorMsg/finishedAt）
- 实体：`ops_human_insights`（洞察知识库：category/driver/emotion/title/content/tags/refAnalysisId/usageCount）
- 索引：`ops_analysis_tasks(tenant_id, status)`、`ops_human_insights(tenant_id, driver)`、`ops_human_insights(tenant_id, emotion)`、`ops_human_insights(tenant_id, usage_count)`
- 合规：`analysis_tasks` 仅存聚合统计、`human_insights` 仅存结论，均不含个人信息（合规边界②）
- 由 TypeORM 同步生成（AUTO=True），也可落 `docs/migrations/003_analyze.sql` 供 CI/生产显式执行。

## 004_topic.sql（E 选题）
- 实体：`ops_topics`（选题：analysisId/attributionId/title/humanDriver/emotion/formulaTags/status/score/abVariantOf/scheduledAt/accountId/promptVersion/modelUsed）
- 唯一约束：无（去重在服务层按 `tenant_id+title+humanDriver+emotion` 判重）
- 索引：`ops_topics(tenant_id, status)`、`ops_topics(tenant_id, human_driver)`、`ops_topics(tenant_id, emotion)`、`ops_topics(tenant_id, ab_variant_of)`、`ops_topics(tenant_id, account_id)`、`ops_topics(tenant_id, analysis_id)`
- 合规：仅存聚合洞察结论与选题元数据，不含个人信息（合规边界②）；`attribution_id` 由 `src/core/attribution-id.ts` 生成
- 由 TypeORM 同步生成（AUTO=True），也可落 `docs/migrations/004_topic.sql` 供 CI/生产显式执行。

## 005_script.sql（F 脚本）
- 实体：`ops_scripts`（脚本：topicId/attributionId/title/content/hook/hookEmotion/spokenTrack/subtitleTrack/templateId/version/parentVersionId/status/complianceRisk/promptVersion/modelUsed）
- `attributionId`：直接复用 E 选题值，F **不重新生成**（链路只读透传，对齐 `src/core/attribution-id.ts`）
- 唯一约束：无（版本由 `parentVersionId` 自引用链表达）
- 索引：`ops_scripts(tenant_id, status)`、`ops_scripts(tenant_id, topic_id)`、`ops_scripts(tenant_id, parent_version_id)`
- 合规：仅存脚本内容/口播/字幕/合规命中，不含个人信息（合规边界②）；违禁词预检阶段1 内嵌 `src/modules/script/script.types.ts` 的 `BANNED_WORDS`，联动 P 阶段迁至违禁词库治理
- 由 TypeORM 同步生成（AUTO=True），也可落 `docs/migrations/005_script.sql` 供 CI/生产显式执行。

## 006_publish.sql（I 发布）
- 实体：`ops_publish_tasks`（发布任务：scriptId/accountId/platform/attributionId/videoId/scheduledAt/status/retryCount/errorMsg/extPostId/cartProductId/cartClicks/orderConv/publishedAt）
- `attributionId`：直接复用 F 脚本值，I **不重新生成**（链路只读透传，对齐 `src/core/attribution-id.ts`）
- `ext_post_id`：平台回执幂等键（阶段1 形如 `pub_<tenant>_<scriptId>_<accountId>`；同 tenant+scriptId+accountId 已 published 视为幂等不重复发）
- 唯一约束：无（幂等由服务层按 tenant+scriptId+accountId+published 判定）
- 索引：`ops_publish_tasks(tenant_id, status)`、`ops_publish_tasks(tenant_id, script_id)`、`ops_publish_tasks(tenant_id, account_id)`、`ops_publish_tasks(tenant_id, ext_post_id)`
- 合规：发布前校验复用 F 脚本 `complianceRisk`（level=high 禁止发布）；仅存发布元数据与转化计数，不含个人信息（合规边界②）
- 由 TypeORM 同步生成（AUTO=True），也可落 `docs/migrations/006_publish.sql` 供 CI/生产显式执行。

## 007_workflow.sql（L 编排）
- 实体：`ops_workflow_defs`（编排：name/nodes(JSON {id,type,config})/edges(JSON {from,to,condition?})/trigger/cronExpr/enabled）、`ops_workflow_runs`（运行：defId/status/progress/startedAt/finishedAt）、`ops_workflow_run_logs`（节点日志：runId/nodeId/nodeType/status/input/output/traceId）
- `attribution_id` 沿链透传（C→D→E→F→I 各节点产出经既有模块透传，编排层不重新生成）
- 唯一约束：无；DAG 合法性在保存时由服务层校验（环检测 + 节点 id 唯一 + 边引用存在）
- 索引：`ops_workflow_defs(tenant_id)`、`ops_workflow_runs(tenant_id, def_id)`、`ops_workflow_run_logs(tenant_id, run_id)`
- 合规：编排仅调度既有模块，不新增数据采集；各节点沿用上游模块合规边界（合规边界①/②）
- 由 TypeORM 同步生成（AUTO=True），也可落 `docs/migrations/007_workflow.sql` 供 CI/生产显式执行。

## 009_n_role.sql（N 团队与权限）
- 实体：`ops_roles`（角色：name/description/permissions(JSON)/isSystem）、`ops_role_user`（用户-角色绑定：userId/roleId，唯一约束 (tenant_id,user_id,role_id)）、`ops_audit_logs`（审计：userId/action/module/resource/traceId/ts）
- 唯一约束：`ops_roles(tenant_id, name)`、`ops_role_user(tenant_id, user_id, role_id)`
- 索引：`ops_audit_logs(tenant_id, ts)`、`ops_roles(tenant_id)`、`ops_role_user(tenant_id)`
- 合规：仅存角色/绑定/操作元数据，不含个人信息（合规边界②）；userId 来自管理系统 SSO 透传，运营系统不持有用户表
- 由 TypeORM 同步生成（AUTO=True），也可落 `docs/migrations/009_n_role.sql` 供 CI/生产显式执行。

## 010_t_selection.sql（T 选品中心）
- 实体：`ops_selection_products`（选品：source/platform/externalProductId/title/commissionRate(DECIMAL 5,2)/reputationScore(DECIMAL 3,2)/sales30d/price/category/humanDriver/metrics(JSON)/collectedAt）、`ops_selection_lists`（选品清单：name/items(JSON number[])）
- 索引：`ops_selection_products(tenant_id)`、`(platform)`、`(external_product_id)`、`(category)`；`ops_selection_lists(tenant_id)`
- `human_driver` 映射 D 字典（7 人性）；仅存选品元数据与清单，不含个人信息（合规边界②）
- 由 TypeORM 同步生成（AUTO=True），也可落 `docs/migrations/010_t_selection.sql` 供 CI/生产显式执行。

## 011_r_product.sql（R 商品内容中心）
- 实体：`ops_products`（商品：sourceType(system/manual/competitor/t_selection)/externalProductId/selectionProductId(→T)/title/stock(库存单一真源)/price/category/humanDriver(人性)）、`ops_product_contents`（AI 内容：productId/humanDriver/titleAi/sellingPoint(映射人性 text)/content(JSON sections)/script(口播话术)/xhsCopy(种草)/version/complianceRisk/status）、`ops_product_detail_pages`（详情页：productId/sections(JSON)）
- 索引：`ops_products(tenant_id)`、`(category)`、`(selection_product_id)`；`ops_product_contents(tenant_id, product_id)`；`ops_product_detail_pages(tenant_id, product_id)`
- `human_driver` 映射 D 字典（7 人性），与 T 选品联动；合规校验为 P 内嵌兜底（基础违禁词），完整词库由 P 模块接入
- 由 TypeORM 同步生成（AUTO=True），也可落 `docs/migrations/011_r_product.sql` 供 CI/生产显式执行。

## 012_k_live.sql（K 直播中心）
- 实体：`ops_live_rooms`（直播间：type(real/digital)/platform/account_id(→B)/rtmp_url/status(created/live/ended)/title/product_ids(→R JSON)/attribution_id(live)/started_at/ended_at）、`ops_digital_humans`（数字人：name/avatar/voice/status）、`ops_live_danmu`（弹幕：room_id/content/is_ai_reply/ai_reply/ts）、`ops_live_ai_replies`（弹幕AI应答：room_id/question/answer/status(auto/pending)）、`ops_live_stats`（实时统计：room_id/online_count/gmv/attribution_id/ts）
- 索引：`ops_live_rooms(tenant_id,status)`、`(tenant_id,account_id)`；`ops_digital_humans(tenant_id,status)`；`ops_live_danmu(tenant_id,room_id)`；`ops_live_ai_replies(tenant_id,room_id)`、`(tenant_id,status)`；`ops_live_stats(tenant_id,room_id)`
- `attribution_id` 在建 room 时生成（live 类，§12），stats 上报透传，供 J 复盘贯通；`account_id`→B、`product_ids`→R 为逻辑关联（跨域不建物理外键）
- 由 TypeORM 同步生成（AUTO=True），也可落 `docs/migrations/012_k_live.sql` 供 CI/生产显式执行。

## 013_s_ad.sql（S 投流管理）
- 实体：`ops_ad_accounts`（投放账户：platform(抖音/微信/快手)/type(千川/ADQ/小店通)/auth_enc(加密凭据)/status(active/expired/banned)）、`ops_ad_campaigns`（计划：account_id/name/plan_type(standard/full_domain/crowd/bid)/audience(JSON)/budget/spend/roi/attribution_id(ad)/status(draft/running/paused/ended)）、`ops_ad_metrics`（指标：campaign_id/date/impressions/clicks/conversions/cost/roi）
- 索引：`ops_ad_accounts(tenant_id,platform,type)`；`ops_ad_campaigns(tenant_id,account_id)`、`(tenant_id,status)`；`ops_ad_metrics(tenant_id,campaign_id)`
- `attribution_id` 在建计划时生成（ad 类，§12），复盘透传，供 J 归因/ W 对账（消耗/ROI）贯通；`account_id`→投放账户为逻辑关联（跨域不建物理外键）
- 由 TypeORM 同步生成（AUTO=True），也可落 `docs/migrations/013_s_ad.sql` 供 CI/生产显式执行。

## 014_u_private.sql（U 粉丝与私域运营）
- 实体：`ops_fans_profiles`（粉丝画像：platform/public_id(公开ID)/level/interact_agg(JSON 聚合)/tags(JSON 分层)/source(aggregate/authorized/public)）、`ops_private_groups`（私域群：name/members(JSON 公开ID)/type(wecom/wechat)）
- 索引：`ops_fans_profiles(tenant_id,platform)`、`(tenant_id,source)`、唯一键 `(tenant_id,platform,public_id)`；`ops_private_groups(tenant_id,type)`
- **合规边界（§11②）**：仅存聚合分布与公开字段，禁止个体隐私（禁精准地理位置/个体画像）；`members` 仅存粉丝公开ID；无物理外键跨域关联
- 由 TypeORM 同步生成（AUTO=True），也可落 `docs/migrations/014_u_private.sql` 供 CI/生产显式执行。

## 015_w_revenue.sql（W 收益与对账）
- 实体：`ops_revenue_records`（收益：source(commission/tip/product)/platform/amount(DECIMAL 12,2)/commission(DECIMAL 12,2)/status(pending/settled)/period(YYYY-MM)/biz_date）、`ops_reconciliations`（对账：period/order_amount/commission_amount/settled_amount/diff/status(matched/diff_found)）、`ops_settlements`（分账：type(org_talent_advertiser)/amount/parties(JSON 各方{role,name,amount})/status(pending/invoiced)/invoice_no）
- 索引：`ops_revenue_records(tenant_id, source)`、`(tenant_id, period)`；`ops_reconciliations(tenant_id, period)`；`ops_settlements(tenant_id, type)`、`(tenant_id, status)`
- 联动：← K 直播成交 / S 投流消耗 / R 商品成交 / U 复购分销佣金；对账 `order_amount`=应收总额、`settled_amount`=已结算、`diff`=差额；分账各方金额合计须==`amount`（服务层 `SETTLEMENT_PARTIES_MISMATCH` 校验）；利润 `profit()` = 收入 - 投流消耗(S `ad_metrics.cost`) - 佣金
- 实际迁移脚本位置：`zhixiang-ops-backend/db/migrations/015_w_revenue.sql`（与既有 `db/migrations/` 系列一致）；由 TypeORM 同步生成（AUTO=True）。

## 016_y_order.sql（Y 订单与物流）
- 实体：`ops_orders`（订单：source(management/platform)/platform/order_id(幂等去重)/product_id(→R)/quantity/amount(DECIMAL 12,2)/commission(DECIMAL 12,2)/status(pending_payment/paid/shipped/completed/refunded)/logistics_status(pending/in_transit/delivered)/attribution_id/buyer_info(加密 TEXT)）、`ops_logistics_tracks`（轨迹：order_id/carrier/tracking_no/status/node/ts）、`ops_waybills`（电子面单：order_id/carrier/tracking_no/print_status(pending/printed)/printed_at）
- 唯一约束：`ops_orders(tenant_id, order_id)`（幂等去重）
- 索引：`ops_orders(tenant_id, status)`、`(tenant_id, platform)`、`(tenant_id, product_id)`；`ops_logistics_tracks(tenant_id, order_id)`；`ops_waybills(tenant_id, order_id)`、`(tenant_id, print_status)`
- 联动：← K 直播成交 / R 商品成交 / S 投流 / U 复购分销；库存扣减/回写经 R `products.stock`（单一真源，防超卖应用层顺序写兜底，分布式锁留待增强）；→ W 对账（订单→佣金结算）；收货信息 AES-256-CBC 加密落库 + 展示脱敏（§11②）；attribution_id 贯通 I/S 挂车转化
- 实际迁移脚本位置：`zhixiang-ops-backend/db/migrations/016_y_order.sql`（与既有 `db/migrations/` 系列一致）；由 TypeORM 同步生成（AUTO=True）。

## 017_aa_customer_service.sql（AA 智能客服中心）
- 实体：`ops_customer_sessions`（会话：channel(live_comment/private_dm/short_video_comment/order_message)/buyer_ref(匿名引用,非 PII,明文)/related_order_id/related_product_id/status(open/transferred/closed)/last_message/message_count）、`ops_customer_messages`（消息：session_id/role(user/ai/agent)/content/intent/confidence）、`ops_support_tickets`（工单：session_id/buyer_ref/issue/status(open/pending/resolved/closed)/priority(low/medium/high/urgent)/assigned_to）、`ops_knowledge_base`（知识库：category(product/order/logistics/faq)/question/answer/source(manual/sync_r/sync_y)）、`ops_cs_settings`（客服设置：enabled_channels(JSON 数组)/transfer_threshold(默认 0.5)/auto_reply_enabled(默认 true)/greeting/working_hours，每租户单条）
- 索引：`ops_customer_sessions(tenant_id, status)`、`(tenant_id, channel)`；`ops_customer_messages(tenant_id, session_id)`；`ops_support_tickets(tenant_id, status)`、`(tenant_id, priority)`；`ops_knowledge_base(tenant_id, category)`；`ops_cs_settings(tenant_id)`
- 联动：← I 情报 / K 直播评论 / U 私域 DM / 短视频评论 / Y 订单留言（多渠道事件驱动创建会话）；AI 回复读 Y 订单/物流（`OrderService.getOrder`/`getLogisticsTrack`）+ R 商品（`ProductService.list`）；AI 生成经能力网关 `SkillGateway.generateText`（源透明）；高风险/投诉意图自动转人工
- 合规边界②：仅存匿名 buyer_ref 关联会话（明文，非 PII），不采集姓名/电话/地址；真实 PII 加密在 Y 的 `buyer_info`；AI 回复标注为 AI 生成
- 实际迁移脚本位置：`zhixiang-ops-backend/db/migrations/017_aa_customer_service.sql`（与既有 `db/migrations/` 系列一致）；由 TypeORM 同步生成（AUTO=True）。

## 018_dashboard.sql（M 决策仪表盘与 BI）
- 实体：`ops_dashboards`（仪表盘配置：name/vidgets(JSON 组件配置数组 `widgets`)，每租户可建多条）
- 索引：`ops_dashboards(tenant_id)`
- M 其余看板（overview/funnel/account-compare/topic-efficiency/human-hook）均为跨域聚合查询，复用 J(`ops_feedback`/`ops_driver_efficiency`) 与各域实体，**不新增业务表**；所有查询 where 携带 `tenant_id` 强隔离
- 合规边界②：M 仅聚合业务指标，不采集/留存任何单条个人信息
- 实际迁移脚本位置：`zhixiang-ops-backend/db/migrations/018_dashboard.sql`（与既有 `db/migrations/` 系列一致）；由 TypeORM 同步生成（AUTO=True）。

## 019_materials.sql（G 素材中心）
- 实体：`ops_materials`（素材：type(image/video/music/subtitle/sticker/avatar)/source(jimeng/keling/local/upload)/url(MinIO)/ratio/tags(JSON)/related_script_id(→F scripts)/status(pending/generated/uploaded/failed)/meta(JSON AI 生成详情)）
- 索引：`ops_materials(tenant_id)`、`(tenant_id, type)`
- 与 F/H 联动：related_script_id 关联 F scripts；→ H 成片素材输入；AI 生成当前经 Skill Gateway(text-generate 占位，源透明)，真实 Media Provider 即梦/可灵集成留阶段3 增强，url 由 Provider 回写
- 合规边界②：不采集隐私；AI 来源对 UX 透明（meta.provider）
- 实际迁移脚本位置：`zhixiang-ops-backend/db/migrations/019_materials.sql`（与既有 `db/migrations/` 系列一致）；由 TypeORM 同步生成（AUTO=True）。

## 020_videos.sql（H 智能成片）
- 实体：`ops_videos`（成片：script_id(→F scripts)/material_ids(JSON→G materials)/ratio/duration/url(MinIO)/review_status(pending/reviewing/passed/rejected)/status(draft/editing/done)/title/meta(JSON 分镜/剪辑命令/合规命中)）
- 索引：`ops_videos(tenant_id)`、`(tenant_id, script_id)`
- 与 F/G/K 联动：script_id 关联 F 脚本（读分镜）；material_ids 关联 G 素材；← K 拆条（直播回放，留增强）；FFmpeg 本地自研剪辑（不依赖第三方）
- 合规边界②：不采集隐私；成片源自自有脚本/素材；合规预检内嵌基础词表（P 阶段补完）
- 实际迁移脚本位置：`zhixiang-ops-backend/db/migrations/020_videos.sql`（与既有 `db/migrations/` 系列一致）；由 TypeORM 同步生成（AUTO=True）。
