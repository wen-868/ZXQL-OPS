# 阿砚 · 完整记忆

> 创建时间：2026-08-07
> 最后更新：2026-08-07
> 职责：后端架构师（后端架构 + API + DDL/迁移 + 服务层 + 能力网关 + 适配层 + 平台总后台）
> 技术栈：NestJS 11 / TypeORM / MySQL(MariaDB) / Redis / TypeScript

## 一、个人职责与定位
运营系统后端负责人。落地 B/C/D/E/F/I/L/J 等模块；维护 skill 能力网关与 integration 适配层；多租户隔离。阶段1 MVP 全链路(B→C→D→E→F→I→L→J)后端已验收完成。

## 二、历史任务总览
- R0（2026-08-07）：成员职责定义与后端工程基线已就位（zhixiang-ops-backend 含通用层、tenant.subscriber、skill、integration；jest 36 passed）。
- R1（2026-08-07）：B 账号矩阵(core) 后端落地。`src/modules/account/**`（AccountEntity/AccountHealthEventEntity/AccountService/AccountController/AccountModule + DTO + account.types）；`shared/crypto.ts`（AES-256-GCM 加密 Token）；错误码 ACCOUNT_NOT_FOUND/DUPLICATE/TOKEN_EXPIRED/UNSIGNED；env.OPS_TOKEN_SECRET；注册 AccountModule。build 通过。REST `/api/ops/accounts/*`（CRUD+health/summary+refresh-token）。定时 `reconcileHealth` 每10分钟掉签/临期巡检。
- R2（2026-08-07）：C 情报采集(core) 后端落地。`src/modules/intel/**`（4 实体 CompetitorEntity/CollectedCommentEntity/CollectTaskEntity/HotSnapshotEntity + IntelService + IntelController + IntelModule + DTO + intel.types）；采集网关 `collector/`（CollectorGateway + CollectorAdapter 接口 + NotImplementedCollectorAdapter 占位 + LocalCollectorAdapter 本地样本）；`rate-limiter.ts`（Redis 令牌桶，按租户+平台 10次/分）；错误码 COMPETITOR_NOT_FOUND/COLLECT_TASK_NOT_FOUND/COLLECT_RATE_LIMITED/COLLECT_SOURCE_LEVEL_INVALID；复用 B 的 Platform 枚举；注册 IntelModule。build 通过。REST `/api/ops/intel/*`（competitors/collect/collected-comments/keywords/mine/hot）。合规：评论仅落白名单字段，隐私(手机/地理/IMEI)剥离为[已脱敏]并记 cleanResult；@Cron 每5秒 processPendingTasks 异步处理采集任务。
- R3（2026-08-07）：D 人性分析与洞察引擎(core) 后端落地。`src/modules/analyze/**`（2 实体 AnalysisTaskEntity(`ops_analysis_tasks`)/HumanInsightEntity(`ops_human_insights`) + AnalyzeService(消费 C 干净评论 is_clean=true → 7×6 归因聚类 prompt §14 JSON Schema → 调 SkillGateway.invoke(text-generate) → 解析聚合 driverCounts/emotionScores/topDrivers/topEmotions/insights → seedInsights 沉淀去重 + 报告聚合) + AnalyzeController(`/api/ops/analyze/*` analysis POST/GET:id/report、insights GET/POST) + AnalyzeModule + DTO + analyze.types(7人性×6情绪字典)）；错误码 ANALYSIS_TASK_NOT_FOUND/ANALYSIS_EMPTY_INPUT（复用 HUMANITY_INVALID/EMOTION_INVALID）；跨模块 forFeature 复用 CollectedCommentEntity 消费 C 评论；@Cron 每5秒 processPendingAnalysis 异步聚类。build 通过。合规边界②：仅聚合统计与洞察结论，无单条个人信息落库。
- R4（2026-08-07）：E 选题引擎(core) 后端落地。`src/modules/topic/**`（TopicEntity(`ops_topics`)+topic.types(状态机 idea→todo→written→shot→published + dead，canTransition 校验)+TopicService(消费 D 洞察库 HumanInsightEntity 按 usageCount 降序 或 指定 AnalysisTaskEntity.insights → 聚合生成选题；去重 同租户+title+driver+emotion 跳过；综合评分 50+usageCount*5 封顶100；A/B 变体派生 防环 abVariantOf 非空则 INVALID_AB_VARIANT_CYCLE；排期绑定 B 账号 AccountEntity 不存在→SCHEDULE_ACCOUNT_NOT_FOUND 终态不可排期)+TopicController(`/api/ops/topic/*` generate POST、topics GET/GET:id/PATCH:id、topics/:id/ab POST、topics/:id/schedule POST)+TopicModule+DTO)；`attribution_id` 由 `src/core/attribution-id.ts`(type=content) 生成；错误码 TOPIC_NOT_FOUND/INVALID_STATUS_TRANSITION/INVALID_AB_VARIANT_CYCLE/SCHEDULE_ACCOUNT_NOT_FOUND（复用 HUMANITY_INVALID/EMOTION_INVALID/ANALYSIS_TASK_NOT_FOUND）；跨模块 forFeature 复用 HumanInsightEntity/AnalysisTaskEntity/AccountEntity。build 通过。合规边界②：仅聚合结论与选题元数据。
- R5（2026-08-07）：F 脚本工坊(core) 后端落地。`src/modules/script/**`（ScriptEntity(`ops_scripts` extends 自定义 BaseEntity 含 tenantId；topicId/attributionId/title/content/hook/hookEmotion/spokenTrack/subtitleTrack/templateId/version/parentVersionId/status/complianceRisk/promptVersion/modelUsed)+script.types(状态机 draft→reviewing→approved→published 含打回/重发 canScriptTransition 校验；ComplianceLevel+ComplianceRisk+hasHighRisk；BANNED_WORDS 内嵌违禁词种子；SCRIPT_TEMPLATES 4套)+ScriptService(消费 E 选题 TopicEntity 按 id+tenantId → 调全局 SkillGateway.invoke(text-generate) 生成 content；hook=首行/前60字、hookEmotion=选题 emotion∈6情绪校验 EMOTION_INVALID；complianceRisk 由内嵌 BANNED_WORDS 预检；状态机流转；发布门禁 level=high→COMPLIANCE_BLOCKED；版本 save 存新 parentVersionId+version+1 / rollback 覆盖；模板库)+ScriptController(`/api/ops/script/*` generate POST、scripts GET/GET:id/PUT:id、scripts/:id/check POST、scripts/:id/version POST、templates GET)+ScriptModule forFeature(ScriptEntity,TopicEntity)+DTO)；`attribution_id` **复用 E 选题值、禁止在 F 重新生成**（链路只读透传）；错误码 SCRIPT_NOT_FOUND/SCRIPT_INVALID_TRANSITION/COMPLIANCE_BLOCKED/SCRIPT_VERSION_REQUIRED/SCRIPT_VERSION_NOT_FOUND（复用 TOPIC_NOT_FOUND/EMOTION_INVALID）；`app.module.ts` 注册 ScriptModule；**注意实体必须 extends `../../database/base.entity` 的 BaseEntity（含 tenantId），勿从 typeorm 引入空 BaseEntity**。build 通过。合规边界②：仅脚本内容/口播/字幕/合规命中。
- R6（2026-08-07）：I 发布与分发(core) 后端落地。`src/modules/publish/**`（PublishTaskEntity(`ops_publish_tasks` extends 自定义 BaseEntity 含 tenantId；scriptId/accountId/platform/attributionId/videoId/scheduledAt/status/retryCount/errorMsg/extPostId/cartProductId/cartClicks/orderConv/publishedAt)+publish.types(PublishStatus 枚举 queued/running/done/failed/published/retry；PUBLISHABLE_SCRIPT_STATUSES=[approved,published])+PublishService(消费 F 脚本 ScriptEntity 按 id+tenantId 须 status∈可发布否则 SCRIPT_NOT_PUBLISHABLE；发布前合规校验复用 F complianceRisk level=high→COMPLIANCE_BLOCKED；按 B 账号 AccountEntity 分发 不存在→PUBLISH_ACCOUNT_NOT_FOUND、platform 不一致→PUBLISH_PLATFORM_MISMATCH；阶段1 独立运行模拟回执 extPostId=`pub_<tenant>_<scriptId>_<accountId>`、status=published、publishedAt；**幂等** 同 tenant+scriptId+accountId 已 published 直接返回；batch 展开多条)+PublishController(`/api/ops/publish/*` publish POST、publish/batch POST、publish/:id GET、publish/:id/funnel GET)+PublishModule forFeature(PublishTaskEntity,ScriptEntity,AccountEntity)+DTO)；`attribution_id` **复用 F 脚本值、禁止在 I 重新生成**（链路只读透传）；错误码 PUBLISH_NOT_FOUND/PUBLISH_ACCOUNT_NOT_FOUND/SCRIPT_NOT_PUBLISHABLE/PUBLISH_PLATFORM_MISMATCH（复用 SCRIPT_NOT_FOUND/COMPLIANCE_BLOCKED）；`app.module.ts` 注册 PublishModule。build 通过。合规边界②：仅发布元数据与转化计数。
- R7（2026-08-07）：L 工作流编排(core) 后端落地。`src/modules/workflow/**`（WorkflowDefEntity/WorkflowRunEntity/WorkflowRunLogEntity(`ops_workflow_defs/runs/logs`)+workflow.types(节点类型 collect/analyze/ideate/script/publish/recycle；topoSort(Kahn)+detectCycle DAG 校验)+WorkflowService(createDef DAG 校验 WORKFLOW_NODE_DUP/EDGE_INVALID/DAG_CYCLE/CRON_REQUIRED；run 拓扑执行串联 C→D→E→F→I：collect→IntelService.processPendingTasks、analyze→AnalyzeService.processPendingAnalysis、ideate→TopicService.generateTopics、script→ScriptService.generateScript、publish→PublishService.publish、recycle→J 占位跳过；**单节点失败隔离** run=partial 而非全败；ctx 透传 topicId/scriptId；缺上游产出→WORKFLOW_MISSING_INPUT；streamRun SSE 每500ms 推送 run+logs)+WorkflowController(`/api/ops` workflows POST/GET、workflows/:id POST、workflows/:id/run POST、workflow-runs/:id/stream SSE)+WorkflowModule 注入 Intel/Analyze/Topic/Script/Publish 模块+DTO)；错误码 WORKFLOW_NOT_FOUND/NODE_DUP/EDGE_INVALID/DAG_CYCLE/CRON_REQUIRED/MISSING_INPUT/NODE_UNKNOWN；`app.module.ts` 注册 WorkflowModule。build 通过。
- R8（2026-08-07）：J 数据监控与回收(core) 后端落地（阶段1 MVP 最后一环，闭合 C→D→E→F→I→J→(D)）。`src/modules/recycle/**`（FeedbackEntity(`ops_feedback`)/RecycleTaskEntity(`ops_recycle_tasks`)/DriverEfficiencyEntity(`ops_driver_efficiency`)+recycle.types(RecycleScope video/account/all、RecycleStatus、RecycleMetrics 五维、响应类型)+RecycleService(回收 I 发布数据→模拟五维四率 feedback 落库 attributionId 透传只读；看板五维四率 /dashboard/overview；人性效能 /dashboard/driver-efficiency 按 人性×情绪 聚合；单视频 /feedback/:videoId 含回流再分析状态；反哺 E `topicService.reweightByEfficiency` 对转化率最高人性命中选题 score+8 封顶100；回流 D `analyzeService.reanalyzeFromRecycle` 将评论注入 collected_comments 建异步分析任务回写 feedback.re_analysis_id 闭环)+RecycleController(`/api/ops` recycle POST、recycle/:id GET、dashboard/overview GET、dashboard/driver-efficiency GET、feedback/:videoId GET、analysis/rerun POST)+RecycleModule forFeature(三 J 实体 + PublishTaskEntity/ScriptEntity/TopicEntity 只读消费，注入 AnalyzeModule/TopicModule)+DTO）；配套 `AnalyzeService.reanalyzeFromRecycle`(D 回流入口)、`TopicService.reweightByEfficiency`(E 反哺入口)、错误码 RECYCLE_TASK_NOT_FOUND/FEEDBACK_NOT_FOUND/RECYCLE_NO_DATA/RECYCLE_TARGET_INVALID；注册 RecycleModule；新增 `db/migrations/008_recycle.sql`。**苏晴发现确凿 Bug 已修**：`runCollection` video scope 对非数字 targetRef 走 ext_post_id 兜底而非抛错（与 account scope 不对称、违反验收）→ 改为直接抛 RECYCLE_TARGET_INVALID。build 通过。合规边界②：仅聚合表现与已脱敏评论，无单条个人信息。

## 三、核心技术沉淀
- 一致性基线：对齐管理系统 `D:\Users\ZXQL\ZXQL-MS\wen-ssystem\backend`（响应信封 `{code,msg,data,traceId}`、AppError、tenantId+AsyncLocalStorage、env 命名；运营专属 `OPS_` 前缀）。
- 启动需本地 MariaDB(3306,--skip-ssl)+Redis(6379)；`node dist/main` 监听 3100，`/api/ops/health` 探依赖。

## 四、提交前自查清单
1. 是否遵循响应信封与 AppError？
2. 是否写入/更新 API接口文档.md（如新增接口）？
3. DDL 是否放 docs/migrations 并更新数据库变更清单？
4. `npm run build` + `npx jest` 是否通过？
5. 是否更新 current-tasks.md 状态与踩坑日志？

## 五、必读文件清单
1. `D:\Users\ZXQL\ZXQL-OPS\zhixiang-ops-workspace\项目规则\项目规则.md`
2. `D:\Users\ZXQL\ZXQL-OPS\zhixiang-ops-workspace\docs\tasks\current-tasks.md`
3. `D:\Users\ZXQL\ZXQL-OPS\zhixiang-ops-workspace\docs\踩坑日志.md`
4. `D:\Users\ZXQL\ZXQL-OPS\zhixiang-ops-workspace\docs\API接口文档.md`
5. `D:\Users\ZXQL\ZXQL-OPS\zhixiang-ops-workspace\docs\memories\阿砚-记忆.md`

## 六、当前关注的问题
- 阶段1 MVP 全链路已完成验收；下一候选 A 权限 或 阶段2 商业化（T/R/K/S/U/W/Y/M）。

## 七、迁移后恢复上下文
读必读清单 → 读 current-tasks.md 属我任务 → 按 API契约开发 → build+test → 回报舟行。
